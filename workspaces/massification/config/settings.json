{
    "id": "servicenow-worker",
    "name": "Servicenow Worker",
    "debug": true,
    "release": {
        "id": "1",
        "name": "v0.0.1"
    },
    "integrations": {
        "mongodb": {
            "socportal": {
                "host": "${os.env['MONGODB_HOST']}",
                "port": "${os.env['MONGODB_PORT']}",
                "user": "${os.env['MONGODB_USER']}",
                "password": "${os.env['MONGODB_PASSWORD']}",
                "database": "${os.env['MONGODB_DATABASE']}"
            }
        }
    },
    "env": {},
    "safe_mode": {
        "active": false,
        "safe_time": 10
    }
}


